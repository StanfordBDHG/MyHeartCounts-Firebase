rules_version = '2';
service cloud.firestore {
  match /databases/{databaseId}/documents {
    function isAuthenticated() {
      return request.auth != null;
    }
  
    function isUser(userId) {
      return isAuthenticated()
        && request.auth.uid == userId;
    }

    match /questionnaires/{questionnaireId} {
      allow read: if isAuthenticated();
      allow write: if false;
    }

    match /users/{userId} {

      allow read: if isUser(userId);

      allow create: if isUser(userId);

      allow update: if isUser(userId);
    }

    match /users/{userId}/{collectionName}/{documentId} {

      // documentSnapshots is server-only snapshot history; this exclusion is
      // the sole rule keeping clients out of it.
      allow read, write: if isUser(userId) && collectionName != "documentSnapshots";
    }

    match /users/{userId}/stats/{document=**} {
      allow read, write: if isUser(userId);
    }
    
    match /feedback/{uuid} {
      allow create: if isAuthenticated() 
                    && request.resource.data.accountId == request.auth.uid;
      
      allow update, delete: if false;
      
      allow read: if false;
    }
  }
}